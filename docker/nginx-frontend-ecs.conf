# ECS: ALB routes api.* to backend, frontend only serves static files
# No /api proxy - browser calls api.roshan-shrestha.com directly
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
