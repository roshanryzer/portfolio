# Use cmd.exe for npm scripts so Unix-style "cmd || exit 0" works on Windows
# (PowerShell does not support || and breaks NestJS/opencollective postinstall)
script-shell=C:\\Windows\\System32\\cmd.exe
